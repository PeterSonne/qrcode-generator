
/**
 * Encoding modes
 */
export enum MODE {
  NUMERIC = 'numeric',
  ALPHA_NUMERIC = 'alpha_numeric',
  BYTE = 'byte',
}

/**
 * Error correction levels
 */
export enum ERROR_CORRECTION_LEVEL {
  LOW = 'L',
  MEDIUM = 'M',
  QUALITY = 'Q',
  HIGH = 'H'
}